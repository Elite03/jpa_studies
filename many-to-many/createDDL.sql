CREATE TABLE PROJECT (ID INTEGER AUTO_INCREMENT NOT NULL, PROJECTNAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE EMPLOYEE (ID INTEGER AUTO_INCREMENT NOT NULL, EMPLOYEENAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PROJECT_EMPLOYEE (Project_ID INTEGER NOT NULL, employees_ID INTEGER NOT NULL, PRIMARY KEY (Project_ID, employees_ID))
CREATE TABLE EMP_PROJ (EMP_ID INTEGER NOT NULL, PROJ_ID INTEGER NOT NULL, PRIMARY KEY (EMP_ID, PROJ_ID))
ALTER TABLE PROJECT_EMPLOYEE ADD CONSTRAINT FK_PROJECT_EMPLOYEE_Project_ID FOREIGN KEY (Project_ID) REFERENCES PROJECT (ID)
ALTER TABLE PROJECT_EMPLOYEE ADD CONSTRAINT FK_PROJECT_EMPLOYEE_employees_ID FOREIGN KEY (employees_ID) REFERENCES EMPLOYEE (ID)
ALTER TABLE EMP_PROJ ADD CONSTRAINT FK_EMP_PROJ_PROJ_ID FOREIGN KEY (PROJ_ID) REFERENCES PROJECT (ID)
ALTER TABLE EMP_PROJ ADD CONSTRAINT FK_EMP_PROJ_EMP_ID FOREIGN KEY (EMP_ID) REFERENCES EMPLOYEE (ID)
